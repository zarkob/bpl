name: Connectivity Tests

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  connectivity:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build parser artifacts
        run: node build-parser.js

      - name: Build web dist
        run: npm run build

      - name: Run connectivity + regression tests
        run: npm test
